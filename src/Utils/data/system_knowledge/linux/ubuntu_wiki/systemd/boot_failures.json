{
  "topic": "Systemd Boot Failures",
  "category": "systemd",
  "description": "Diagnosing and fixing system boot issues caused by systemd services or units on Ubuntu systems.",
  "symptoms": [
    "System stuck during boot",
    "Black screen after GRUB menu",
    "Emergency mode prompt",
    "Excessively long boot time"
  ],
  "diagnosis": [
    {
      "step": "Check failed units",
      "command": "systemctl --failed",
      "notes": "Lists systemd units that failed during boot"
    },
    {
      "step": "Inspect current boot logs",
      "command": "journalctl -b",
      "notes": "Shows logs from the current boot session"
    },
    {
      "step": "Inspect previous boot logs",
      "command": "journalctl -b -1",
      "notes": "Useful when the current boot is unstable or incomplete"
    }
  ],
  "solutions": [
    {
      "fix": "Disable problematic service",
      "command": "systemctl disable <service>",
      "notes": "Use when a service repeatedly blocks the boot process"
    },
    {
      "fix": "Mask a broken unit",
      "command": "systemctl mask <service>",
      "notes": "Prevents the unit from starting entirely, even if required by others"
    },
    {
      "fix": "Boot into rescue mode",
      "command": "systemctl rescue",
      "notes": "Starts a minimal environment suitable for system recovery"
    }
  ],
  "references": [
    "https://help.ubuntu.com/community/Systemd",
    "https://help.ubuntu.com/community/BootRepair"
  ]
}
