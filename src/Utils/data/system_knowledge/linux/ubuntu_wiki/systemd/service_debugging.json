{
  "topic": "Debugging systemd Services",
  "category": "systemd",
  "description": "Steps to inspect, debug, and fix failing or misbehaving systemd services on Ubuntu systems.",
  "symptoms": [
    "Service fails to start",
    "Service exits immediately",
    "Repeated restarts",
    "Dependency-related startup errors"
  ],
  "diagnosis": [
    {
      "step": "Check service status",
      "command": "systemctl status <service>",
      "notes": "Displays service state, exit codes, and recent log messages"
    },
    {
      "step": "View service logs",
      "command": "journalctl -u <service>",
      "notes": "Shows logs specific to the selected service"
    },
    {
      "step": "Inspect service dependencies",
      "command": "systemctl list-dependencies <service>",
      "notes": "Helps identify missing or failed dependency units"
    }
  ],
  "solutions": [
    {
      "fix": "Restart the service",
      "command": "systemctl restart <service>",
      "notes": "May resolve transient or startup ordering issues"
    },
    {
      "fix": "Create or edit service override",
      "command": "systemctl edit <service>",
      "notes": "Safely customizes service behavior without modifying vendor files"
    },
    {
      "fix": "Reload systemd daemon",
      "command": "systemctl daemon-reexec",
      "notes": "Ensures systemd reloads unit definitions cleanly"
    }
  ],
  "references": [
    "https://help.ubuntu.com/community/Systemd",
    "https://manpages.ubuntu.com/manpages/systemctl.1.html"
  ]
}
